extends layout.pug

block scripts
    style.
        body {
        font-family: "Lato", sans-serif;
        }
        .sidenav {
        max-width: 28%;
        position: fixed;
        z-index: 1;
        top: 8%;
        left: 10px;
        background: #eee;
        overflow-x: hidden;
        padding: 8px 0;
        }
        .sidenav a {
        padding: 6px 8px 6px 16px;
        text-decoration: none;
        font-size: 22px;
        color: #2196F3;
        display: block;
        }
        .sidenav a:hover {
        color: #064579;
        }
        .main {
        margin-left: 140px; /* Same width as the sidebar + left position in px */
        font-size: 28px; /* Increased text to enable scrolling */
        padding: 0px 150px;
        }
        @media screen and (max-height: 450px) {
        .sidenav {padding-top: 15px;}
        .sidenav a {font-size: 18px;}
        }

block content
    body
        .sidenav
            a(href='#') My Orders
            a(href='#') Dashboard (admin)
            a(href='#') Add Sale Item (admin)
            a(href='#') Sign Out
        .main
            h2(style='text-align: center;') My Orders
