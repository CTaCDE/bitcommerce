extends layout.pug


block scripts
    link(href='stylesheets/myaccount.css', rel='stylesheet', type='text/css')
    block subscripts

block content
    .container(style="margin-left:28%")
        .navbar.navbar
            .container-fluid
                .navbar-header
                    .nav.navbar-nav.collapse.navbar-collapse(style="font-family: 'Playfair Display', serif; font-size: 20px")
                        li
                            a(href='/orderhistory') My Orders
                        if(user)
                            if(user.role === 'admin')
                                li
                                    a(href='/logs') Dashboard
                                li
                                    a(href='/additems') Add Sale Item
                        li
                            a(href='/submitdesign') Submit a Design
                        li
                            a(href='/logout') Logout
    hr
            

    block subcontent